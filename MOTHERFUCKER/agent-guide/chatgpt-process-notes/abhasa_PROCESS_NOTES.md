# Process notes

Each pack is a deterministic 6-scene visual essay. The design uses sparse text, symbolic geometry, slow reveals and reusable scene factories. The MP4 is intended as a silent visual bed or as six cuttable clips. `scene_manifest.json` contains exact boundaries.
